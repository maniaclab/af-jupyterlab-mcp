apiVersion: v1
kind: Service
metadata:
  name: {{ include "jupyterlab-mcp.fullname" . }}
  labels:
    {{- include "jupyterlab-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "jupyterlab-mcp.selectorLabels" . | nindent 4 }}
