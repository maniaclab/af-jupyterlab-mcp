apiVersion: v1
kind: Service
metadata:
  name: {{ include "af-jupyterlab-mcp.fullname" . }}
  labels:
    {{- include "af-jupyterlab-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
  selector:
    {{- include "af-jupyterlab-mcp.selectorLabels" . | nindent 4 }}
